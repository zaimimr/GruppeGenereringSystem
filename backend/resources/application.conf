ktor {
    deployment {
        port = 8000
        port = ${?PORT}
        autoreload = true
        watch = [ backend ]
    }
    application {
        modules = [ com.gruppe7.ApplicationKt.module ]
    }
}
